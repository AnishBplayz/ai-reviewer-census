# Optional. If unset, the collector falls back to `gh auth token`.
#
# A classic personal access token with the `public_repo` scope is enough — the
# collector only ever reads public pull requests and never writes to GitHub.
# https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here
